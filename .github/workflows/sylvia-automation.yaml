name: .github
on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - "*"

jobs:
  newbie-devOps-engineer-1:
    runs-on: ubuntu-latest
    steps:
      - name: setup virtual network
        uses: actions/checkout@v3

      - name: do all the aws setup
        run: echo "Hello, world!"

  newbie-devOps-engineer-2:
    runs-on: ubuntu-latest
    steps:
      - name: setup ec2 and connect your ip to server
        uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo "Hello, world!"

  newbie-devOps-engineer-3:
   runs-on: ubuntu-latest
   steps:
     - name: upload webcontent to mobaxterm
       uses: actions/checkout@v3

     - name: do all the aws setup
       run: echo "Hello, world!"
